@model List<Application.ViewModels.ProducerViewModel>

@{
    ViewData["Title"] = "Producers";
}

<div class="container mt-5">
    <h3 class="text-white text-center mb-4">Productoras</h3>

    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
            <div class="d-flex justify-content-end">
                <a asp-controller="Producer" asp-action="Create" class="btn btn-sm btn-success">
                    Nuevo <i class="fas fa-plus text-white"></i>
                </a>
            </div>
            @if (Model == null || !Model.Any())
            {
                <h3 class="text-white text-center">No se encontraron productoras</h3>
            }
            else
            {
                <table class="table table-dark table-striped mt-3">
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th class="text-end">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Name</td>
                                <td class="text-end">
                                    <a class="text-white me-3" asp-controller="Producer" asp-action="Edit" asp-route-id="@item.Id">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a class="text-white" asp-controller="Producer" asp-action="Delete" asp-route-id="@item.Id">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
        </div>
    </div>
</div>